name: Scrape Konzerthaus Berlin

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to scrape from and commit to"
        required: false
        default: "main"

permissions:
  contents: write

concurrency:
  group: scrape-konzerthaus-berlin
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch || 'main' }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --quiet crawl4ai pydantic anthropic fastapi
          python3 -m playwright install --with-deps chromium

      - name: Run scraper
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          PYTHONUNBUFFERED: "1"
        run: python3 -m backend.scrape.run_scraper konzerthaus_berlin

      - name: Show summary
        run: |
          python3 -c "
          import json
          with open('backend/konzerthaus_berlin_events.json') as f:
              data = json.load(f)
          total = data.get('total_events', 0)
          print(f'Total events: {total}')
          for e in data.get('events', [])[:5]:
              print(f"  {e.get('date','')}: {e.get('title','')[:60]}")
          "

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend/konzerthaus_berlin_events.json
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "Refresh konzerthaus_berlin_events.json (manual scrape)"
          BRANCH="${GITHUB_REF_NAME:-main}"
          for i in 1 2 3; do
            git pull --rebase --autostash origin "$BRANCH" || true
            if git push origin "HEAD:$BRANCH"; then exit 0; fi
            sleep $((i * 3))
          done
          exit 1
